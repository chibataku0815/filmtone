# 初回: cp .notary.env.example .notary.env
# その後 .notary.env の REPLACE_ME を 1Password の値に置換（.notary.env は git に載せない）

export APPLE_ID="REPLACE_ME"
export APPLE_APP_SPECIFIC_PASSWORD="REPLACE_ME"
export APPLE_TEAM_ID="REPLACE_ME"
