# App Store Screenshots

Put localized App Store screenshots here before running the `release` lane.

- Use `bundle exec fastlane ios screenshots` to drive snapshot once a UI test scheme exists.
- `ja` and `en-US` are the supported storefront locales in this rail.
- The `release` lane skips screenshot upload when this folder only contains scaffolding.
