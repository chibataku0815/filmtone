# Filmtone iOS v1.5 — Metal Glow Prototype Handoff

作成: 2026-05-03 JST
worktree: `.claude/worktrees/ios-v1.5-export-profile`
branch: `feature/ios-v1.5-export-profile`
prototype scope: GlowFamily のみ Metal 化（Phase 1）。Vignette / EdgeOptics は Phase 2 へ送り。

## Status Summary（review 後の正確な表記）

- **Performance gate: passed.** export 193.2s → 95.6s (-50.5%)、`acceleratedRenderStages == ["GlowFamily/metal"]` を実機 sidecar で確認済。
- **Quality gate: pending.** 視覚 A/B (Full CI vs Metal Glow の同一フレーム比較) 未実施。
- **Ship-ready: NO.** 視覚 A/B が通るまで Phase 1 を「成功」扱いしない。視覚 A/B が通って初めて Phase 2 設計に入る権利が生まれる。

## What Changed

新規ファイル:

- `apps/capacitor-film-lab-ios/ios/App/App/FilmtoneMetalOpticsRenderer.swift`
  - MTLDevice / MTLCommandQueue / MTLLibrary を所有する renderer。
  - Metal source は Swift 文字列 inline（`device.makeLibrary(source:)` で
    runtime compile）。`.metal` ファイルを project に足さなくていいので
    pbxproj 改変が 4 セクション追加のみで済む。
  - 6 個の compute pipeline:
    `filmtoneSoftKneeHighlight` / `filmtoneTentDownsample` /
    `filmtoneTentUpsample` / `filmtoneCopyTexture` /
    `filmtoneTentUpsampleAccumulate` / `filmtoneGlowComposite`
  - mip kernel 重みは既存 GLSL (COD: AW dual-filter) と同じ
    13-tap downsample / 9-tap upsample。
  - texture pool は 1 export 内で再利用（output 寸法と mip levels が
    変わらないため、初回 alloc 後はゼロアロケーションで回せる）。
  - texture format は `.rgba16Float`、CIContext working color space
    (linear sRGB) で round-trip。

修正:

- `apps/capacitor-film-lab-ios/ios/App/App/FilmtoneExportSession.swift`
  - 環境変数 `FILMTONE_EXPORT_METAL_OPTICS=1` を読む
    `useMetalOpticsForExport: Bool` を追加。
  - `metalOpticsRenderer` を lazy var で保持。
  - `applyGlowFamilyStage` の冒頭に Metal gate を挿入:
    - flag ON
    - `disableGlowFamilyForExport == false`
    - `request.sourceKind == .video`
    - `request.renderMode == .quality`
    - `loadedDepthMap == nil`（depth path は CI 維持）
    - renderer が初期化済み
    - `renderer.renderGlow(...)` が `nil` を返さない
  - 全部満たした時だけ Metal 結果に置き換える。失敗時は何もせず CI Glow に
    そのまま落ちる。
  - sidecar への新フィールド `acceleratedRenderStages` 連携。
- `apps/capacitor-film-lab-ios/ios/App/App/FilmtoneExportSidecarBuilder.swift`
  - `SidecarPerformance.acceleratedRenderStages: [String]?` を追加。
  - `disabledRenderStages` と同じ慣習で `nil` 既定（後方互換）。
- `apps/capacitor-film-lab-ios/ios/App/App.xcodeproj/project.pbxproj`
  - 新 Swift ファイルを 4 セクション登録（UUID `...002E`）。

無関係な既存 dirty diff（mezzanine cache 周りなど）は触っていない。

## Existing Flag Hygiene

ユーザー指示通り:
- `FILMTONE_EXPORT_DISABLE_GLOW_FAMILY` は維持（Glow 完全 OFF baseline 用）。
- EdgeOptics / Vignette OFF flag は無し。`rg` 確認済 (空マッチ)。

## Verification (Local)

すべて green:

- `git diff --check`
- `bun run verify:swift-contract`
- `xcodebuild -workspace ios/App/App.xcworkspace -scheme App -destination 'generic/platform=iOS Simulator' -configuration Debug build CODE_SIGNING_ALLOWED=NO`
- `xcodebuild -workspace ios/App/App.xcworkspace -scheme App -destination 'id=3A2A3A66-D092-5F87-8CE7-9A1EBD238FE9' -configuration Release -allowProvisioningUpdates build`
- `xcrun devicectl device install app --device 3A2A3A66-D092-5F87-8CE7-9A1EBD238FE9 .../Release-iphoneos/App.app`

実機 export はまだ。次チャットでユーザーが flag ON にして export を回す。

## How To Run The Prototype Export

iOS は env var を Xcode scheme 経由で渡す必要がある（devicectl install 後の
springboard launch では env var は届かない）。

1. Xcode で `ios/App/App.xcworkspace` を開く。
2. `Product → Scheme → Edit Scheme... → Run → Arguments`。
3. `Environment Variables` に追加:
   - `FILMTONE_EXPORT_METAL_OPTICS = 1`
4. （任意）以前追加していた `FILMTONE_EXPORT_DISABLE_GLOW_FAMILY` は外す
   または `0` に。両方 ON だと Glow が完全 OFF になり Metal path には
   入らない（gate で `!disableGlowFamilyForExport` を要求しているため）。
5. iPhone を Mac に繋いで Xcode から `Product → Run`（実機 Release config
   推奨だが、scheme 上で環境変数を渡す関係で Debug でも可。
   速度比較は同じ Run 設定で揃える）。
6. 既存と同じ素材 `A001_03270631_C008.mov` で Quality export。
7. 起動時 NSLog に
   `FilmtoneExportSession: Metal optics prototype enabled by FILMTONE_EXPORT_METAL_OPTICS (Quality video only)`
   が出ているか xcrun devicesyslog or Xcode console で確認。

### Sidecar Pull

```bash
xcrun devicectl device copy from \
  --device 3A2A3A66-D092-5F87-8CE7-9A1EBD238FE9 \
  --domain-type appDataContainer \
  --domain-identifier com.chibatakumi.film.lab.ios \
  --source Library/Caches/FilmtonePhase0/exports \
  --destination /tmp/filmtone-export-profile/device-exports-latest
```

### What To Look For In The Sidecar

```jsonc
{
  "performance": {
    "exportElapsedMs": <number>,
    "renderMs": <number>,
    "avgRenderMsPerFrame": <number>,
    // 新フィールド: Metal が走った場合だけ生える
    "acceleratedRenderStages": ["GlowFamily/metal"]
    // disabledRenderStages は不在のはず（Glow OFF と同時に使うのは矛盾）
  }
}
```

`acceleratedRenderStages` が無い → gate のどれかで弾かれている（もしくは
renderer init が失敗）。NSLog で原因を確認する:
- `FilmtoneMetalOpticsRenderer: Metal device unavailable`
- `FilmtoneMetalOpticsRenderer: shader compile failed: ...`
- `FilmtoneMetalOpticsRenderer: pipeline state creation failed: ...`

## Pinned Baselines (For Comparison)

直近 sidecar から実測:

| 状態 | export elapsed | render | avgRender/frame | thermal |
|---|---:|---:|---:|---|
| 通常 (full CI chain) | 193,197 ms | 180,844 ms | 48.21 ms/frame | nominal→nominal |
| `DISABLE_GLOW_FAMILY=1` | 85,650 ms | 76,285 ms | 20.34 ms/frame | nominal→nominal |
| 差分 (Glow chain 単独) | -107,547 ms | -104,559 ms | -27.87 ms/frame | — |

Glow chain は CI で約 105s（render 時間ベース）。Metal で 5-15s に
落とせれば期待 export elapsed は約 95-105s。

## Measured Outcome（review 訂正版）

実機実測 (iPhone 17 Pro, A001_03270631_C008.mov, Quality 1920x1080):

| 状態 | export elapsed | render | avgRender/frame | 解釈 |
|---|---:|---:|---:|---|
| Full CI (baseline) | 193.2 s | 180.8 s | 48.21 ms | 出荷 production path |
| Glow OFF (上限効果検証) | 85.7 s | 76.3 s | 20.34 ms | **視覚的に Glow 不在 / 出荷不可**。"どこまで詰められるか" の上限値 |
| Metal Glow prototype | 95.6 s | 43.8 s | 11.67 ms | Glow 視覚あり、export elapsed -50.5% |

**読み方の注意（review 指摘の誤解を防ぐ）**:

1. **`renderMs` だけ見ると Metal Glow (43.8s) は Glow OFF (76.3s) より速い** ように見えるが、**`exportElapsedMs` では Metal Glow (95.6s) は Glow OFF (85.7s) より約 10s 遅い**。これは Metal renderer 内で `ciContext.render(input, to: inputTexture, commandBuffer:)` が上流 CI graph を eager 評価し、その分が `renderMs` から `buildGraphMs` (5.8s → 48.6s) に移ったため。仕事量自体は減ったが、bucket の付け替えが起きている。
2. ユーザー体感では「Glow OFF より約 10s 遅いが Glow が視覚として戻っている」が正確。Glow OFF は出荷可能形態ではないので、これは想定通りで OK。
3. Full CI (193.2s) と比べた -97.5s こそがこの prototype の正味勝ち分。

## Phase 2 Plausibility（review 訂正版）

Phase 2 で Vignette + EdgeOptics + Grain を Metal 化した場合の予測:

- **試算 (sub-stage forced profile より)**: Vignette ~30s、EdgeOptics ~16s、Grain ~5s。合計 ~50s 削減候補。
- **caveat**: sub-stage forced profile は CI fusion を壊して計測するため、stage の **ranking には信頼できるが sizing には信頼できない**。GlowFamily が forced 推定 ~50s incremental に対し実測 ~105s だった事例が直前の lane でそのまま起きている。Vignette / EdgeOptics / Grain の実測コストも 1.5-2x ブレ得る。
- **handoff cost の挙動**: Phase 2 でも CI→MTLTexture handoff は 1 回残る (`applyEdgeOpticsStage` 入口)。upstream CI chain は 1 stage 短くなる (EdgeOptics が Metal 側へ) ので buildGraph は若干軽くなるはずだが、eager render の固定 overhead がどれだけ縮むかは実証なしには断定不可。
- **目標値**: 60-70s は **約束ではなく empirical gate**。Vignette を最初に Metal 化して "実測 vs 予測 ~30s 削減" を見て、ズレの方向で次手を決める。

| Scenario | Phase 2 着地点 |
|---|---:|
| 予測通り全部削れる | 60-65s |
| Vignette だけ予測通り、Edge/Grain は controlled 1.5x miss | 70-80s |
| GlowFamily と同じ 2x miss が他ステージにも起きる | 90-100s (Phase 1 と同程度) |

## Visual Equivalence Argument

Metal 側の数式は CI kernel の **直接ポート**:

| CI Kernel | Metal Kernel | 数式一致? |
|---|---|---|
| `softKneeHighlight` (CIColorKernel) | `filmtoneSoftKneeHighlight` | ✓ 完全一致 |
| `tentDownsample` (CIKernel, 13-tap) | `filmtoneTentDownsample` | ✓ 13-tap 重み一致 |
| `tentUpsample` (CIKernel, 9-tap) | `filmtoneTentUpsample` | ✓ 9-tap 重み一致 |
| `glowComposite` (CIColorKernel) | `filmtoneGlowComposite` | ✓ shoulder + headroom 一致 |
| `weightedImage` + `addImages` | `filmtoneTentUpsampleAccumulate` | ✓ fused 1 dispatch 化 |
| mip pyramid loop | `encodeMipPyramid` | ✓ 同じ accumulation 順序 |
| `halationColor(for: hue)` | `halationColor(forHue:)` | ✓ 同 RGB lerp |
| `computeMipWeights(radius, levels)` | 同 `Self.computeMipWeights` 模写 | ✓ |

差分の余地:
- `half`(fp16) vs CI working format (`RGBAh`/fp16) — 桁落ちはほぼ等価。
- mirror addressing の境界処理は pixel-space 手書きで CI の
  `mirrorCoord` と同等の結果（同じ折り返し式）。
- bilinear interpolation は手書き（Metal `texture2d::read` のみ使用、
  sampler を使わずに 4-tap 加重）。CI 側も同じ 4-tap だが内部実装で
  わずかな丸めが違う可能性あり → 視覚上は識別不能。

A/B 確認は実機 export 後に同フレーム (0%, 25%, 50%, 75%, highlight-heavy)
で並べる。代表素材 `A001_03270631_C008.mov` は強光源あり（Quality export
の bloom が出やすい）ので適切。

## Remaining Visual Risks

1. **Diffusion plate path**: 既存 CI は `image` (フルフレーム) を直接 mip
   blur するが、Metal 側もそうする。同じパス。✓
2. **Halation tint** は hue=0 で `#e81020`、hue=100 で `#c86010` の
   RGB lerp。Metal 側 `halationColor(forHue:)` で同じ係数を計算して
   `SIMD3<Float>` で渡す。✓
3. **Composite shoulder**: `1 - exp(-energy)` は Metal `exp` で同じ。
   highlight headroom は `floor + (1-floor) * sqrt(1-luma)` で同じ。✓
4. **Mip levels** はサイズ違いで丸めが入るので、CI と Metal で 1px ずれる
   可能性あり。視覚的には目立たないはず。
5. **clampedToExtent / cropped**: CI 側は extent 外を clamp する。Metal
   側は mirror addressing。境界数 px の僅差が出る可能性。視覚 OK の想定。

## Known Limitations Of This Prototype

- **Quality video のみ**。Speed / preview / still image / depth-on は
  全部 CI Glow path（gate で fallback）。
- **Vignette / EdgeOptics / Grain は CI のまま**。Phase 2 で同 renderer に
  追加する予定。
- **renderer init 失敗時は silent fallback**（`feedback_no_fallback_bug_hotbed`
  に少し抵触するが、これはレンダラ自体の有無の話で、画質を silent に
  下げる fallback ではない。NSLog で識別可能）。
- **A/B 静止画ダンプ機能は今回足していない**。ユーザーが full export を
  並べて目視で比較する。差が見える場合は次チャットで A/B dump 機能を
  追加（Quality export の特定フレームを画像として保存する小機能）。

## Decision Point: Phase 2 Trigger

実機 sidecar は OK (95.6s 確認済)。**残るのは視覚 A/B 一点**。次の判断:

| 視覚 A/B 結果 | 次手 |
|---|---|
| Full CI と Metal Glow が同等に見える | Phase 2 設計開始 (Vignette + EdgeOptics + Grain) |
| 差が見えるが軽微 (例: bloom 強度の半段ズレ等) | Metal kernel の数値ポートを GLSL と diff、丸め誤差箇所を特定してから Phase 2 |
| はっきり差が見える (halo の形状 / hue) | Phase 2 凍結。Metal kernel の math ポートを根本見直し (mirror 境界 / mip 丸め / fp16 精度) |

視覚 A/B が通らない限り、Phase 1 は **prototype のままで ship しない**。
Phase 1 単独で export -50.5% 削減は確認済だが、画質保証なしに出荷判断はしない。

## Files Touched (commit candidate set)

```
M apps/capacitor-film-lab-ios/ios/App/App.xcodeproj/project.pbxproj
M apps/capacitor-film-lab-ios/ios/App/App/FilmtoneExportSession.swift
M apps/capacitor-film-lab-ios/ios/App/App/FilmtoneExportSidecarBuilder.swift
A apps/capacitor-film-lab-ios/ios/App/App/FilmtoneMetalOpticsRenderer.swift
A docs/filmtone/ios/archive/legacy-handoffs-2026-04-20-to-2026-05-03/v1.5-metal-glow-prototype-handoff-2026-05-03-jst.md (this)
```

無関係な dirty 差分（AssetPickerService / CacheStore / FilmtoneEditorFacade /
MezzanineService / record-export-hardware-trace.sh / 既存 handoff doc 群）は
今回の commit には含めない方針（前ハンドオフから引き継いだ profile 系
instrumentation で、Metal Glow とは独立に判断するべき）。

→ **訂正**: bundle-in-flight feedback memory に従い、上記 dirty 差分も
含めて 1 commit (`3639f78`) にまとめた。

## Post-Review Corrections (commit followup)

レビューで指摘を受けた 4 点に対して以下を反映:

1. **encodeClear の read/write エイリアス除去**: `filmtoneSoftKneeHighlight`
   を inTex/outTex 同一 texture で再利用していた箇所を、専用
   `filmtoneClearTexture` (write-only) に置換。今回のテスト素材は
   bloom/halation/diffusion 全 active で latent だったが、Phase 2 で
   renderer が拡張される前に必須の修正。
   `FilmtoneMetalOpticsRenderer.swift` の `clearTexturePS` 追加と
   `encodeClear` 簡略化、Metal source に `filmtoneClearTexture` 追加。
2. **`buildGraphMs` の解釈訂正**: 旧文言「大半が Metal 内部時間に統合される」
   を削除。Phase 2 でも CI→MTLTexture handoff は 1 回残ることを明記。
3. **「Glow OFF より速い」の修正**: render bucket 単体での比較は技術的に
   正しいが、`exportElapsedMs` では Metal Glow (95.6s) > Glow OFF (85.7s)
   である事実を併記。bucket 移動の現象を明示。
4. **60-70s 予測の確度**: 「目標は約束ではない」「sub-stage forced profile は
   ranking 用で sizing 用ではない」を明記。Phase 2 着地点を 3 シナリオ
   (60-65s / 70-80s / 90-100s) で表化。
5. **視覚 A/B の status**: Phase 1 は "performance gate passed, quality gate
   pending" として "Status Summary" セクションを追加。視覚 A/B が通らない
   限り Phase 2 へ踏み出さない。

これらは follow-up commit で実装。`FilmtoneMetalOpticsRenderer.swift` の
変更は build green (verify:ios + simulator + Release device build)。
