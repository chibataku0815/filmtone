# Desktop v1.11 / iOS 1.10 Release Verification

Milestone: Desktop M13 release verification + iOS 1.10 candidate release
Opened: 2026-05-17 JST

## Goal

Verify current main end to end, publish the next Desktop release if the signed
artifact passes, and continue the iOS 1.10 release rail without mixing public
Desktop truth and App Store iOS truth.

## Edit Targets

- Desktop release metadata, release notes, and release artifacts under
  `apps/filmtone-desktop-macos/`
- iOS release rail under `apps/capacitor-film-lab-ios/`
- Release state notes under `docs/filmtone/desktop/native-desktop-v2/` and
  `docs/filmtone/ios/`

## Read-Only References

- `apps/filmtone-desktop-macos/README.md`
- `docs/filmtone/desktop/README.md`
- `docs/filmtone/desktop/native-desktop-v2/strategy.md`
- `docs/filmtone/desktop/release-cutover/README.md`
- `apps/capacitor-film-lab-ios/CLAUDE.md`
- `apps/capacitor-film-lab-ios/RELEASE.md`

## Checklist

- [x] Confirm release truth for Desktop and iOS before version claims.
- [x] Run current main verification for core, Desktop, iOS, and automation.
- [x] Prepare Desktop v1.11 version/release notes if verification passes.
- [x] Build, sign, notarize, staple, and assess Desktop v1.11 DMG.
- [x] Publish Desktop v1.11 DMG and update metadata.
- [x] Verify iOS 1.10 build 13 release state and run iOS release checks.
- [x] Submit or monitor iOS 1.10 according to live App Store Connect state.
- [x] Re-run truth scripts and record final release state.

## Verification

- `bun run build:core`
- `bun run verify:desktop`
- `bash apps/filmtone-desktop-macos/Verify/run.sh`
- `bun run build:filmtone-automation`
- `bun run verify:ios`
- `git diff --check`
- `bun run check:filmtone-context`
- Desktop release preflight and Gatekeeper assessment commands from the release
  cutover lane.

## Done Conditions

- Desktop public update metadata reflects the released Desktop artifact, or an
  exact release blocker is recorded.
- iOS 1.10 public/review state is verified and advanced when possible, or an
  exact ASC blocker is recorded.
- The current worktree state is clean except unrelated pre-existing files.

## Stop Conditions

- Done conditions met.
- Signing, notarization, upload, or ASC credentials fail in a way that needs
  account owner action.
- Three consecutive failures on the same verification command.

## Out Of Scope

- Broad public copy refresh beyond required release notes / metadata.
- UI warning work for Rec.709-safe Looks.
- Portfolio submodule bump unless needed after successful public release.

## Unexpected Blockers

- None yet.

## Execution Log

- `bun run build:core` passed.
- `bun run verify:desktop` passed before and after the Desktop v1.11 version
  bump.
- `bun run verify:ios` passed.
- `bash apps/filmtone-desktop-macos/Verify/run.sh` passed: `154/154`.
- `bun run build:filmtone-automation` passed.
- `scripts/release-macos.sh 1.11` produced a notarized and stapled
  `Filmtone.app`; `spctl` accepted it as Notarized Developer ID.
- `scripts/package-dmg.sh 1.11` produced a signed, notarized, stapled, and
  Gatekeeper-accepted `Filmtone-1.11.dmg`.
- DMG SHA-256:
  `f72e9b39e3db319e8f5a426df0889ec797a66c857e7b7512f9539a9c8b091c7f`.
- Uploaded DMG to Vercel Blob:
  `https://ehi6m41cp33jiopb.public.blob.vercel-storage.com/filmtone/desktop/Filmtone-1.11.dmg`.
- Deployed portfolio production:
  `https://chibatakumi-portfolio-m9dgxzs2o-forestones-projects.vercel.app`,
  aliased to `https://www.chibatakumi.studio`.
- Uploaded Desktop update metadata with `latestVersion: "1.11"`.
- App Store Connect reports iOS `1.10 (13)` as `READY_FOR_DISTRIBUTION`.
  US / GB / CA / AU / DE / FR / KR iTunes lookups return `1.10`; JP lookup
  still returns `1.9`.

## Copy / History Impact

- Public copy update required: Desktop v1.11 release notes exist and public web
  release surfaces were updated/deployed to v1.11 after the update metadata
  published.
- Implementation history update required: no. This release changes
  source-aware built-in Look behavior but does not change the WebGPU / WebGL,
  React + Capacitor, native SwiftUI / AVFoundation history.
- Release/App Store claim: Desktop truth reports public `latestVersion: 1.11`.
  App Store Connect reports iOS `1.10 (13)` as `READY_FOR_DISTRIBUTION`; public
  storefront lookup is country-specific during propagation, with JP still
  returning `1.9` while checked non-JP storefronts return `1.10`.
- Article Opportunity: Release-note only.
- Change-History Opportunity: Context paragraph, because source-aware Look
  behavior clarifies the difference between source input handling and creative
  Look treatment.
