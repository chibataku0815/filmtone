# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
# Workspace-local installs (nested packages are not covered by root /node_modules)
packages/**/node_modules/

/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# Local agent / browser tooling (never commit)
.playwright-cli/

# film-lab-renderer ships types from dist but build output is generated (like core, keep out of git)
packages/film-lab-renderer/dist/

# Nested `git worktree` checkouts under this repo (do not add to parent index)
.worktrees/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env*.local
